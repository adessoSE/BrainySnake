package de.adesso.brainysnake.playercommon;

public enum RoundEvents {
    MOVED,
    CONFUSED,
    COLLISION_WITH_LEVEL,
    BIT_AGENT,
    BIT_BY_AGENT,
    BIT_HIMSELF,
    DIEDED,
    CONSUMED_POINT,
}
