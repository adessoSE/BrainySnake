package de.adesso.brainysnake.playercommon;

public enum RoundEvent {
    MOVED,
    CONFUSED,
    COLLISION_WITH_LEVEL,
    BIT_AGENT,
    BIT_BY_PLAYER,
    BIT_HIMSELF,
    DIEDED,
    CONSUMED_POINT,
}
